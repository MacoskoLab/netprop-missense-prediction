#!/usr/bin/env bash

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"

if [[ -z $1 ]];
then
    cores=$(nproc)
else
    cores=$1
fi

set -x
snakemake --use-conda --cores $cores
